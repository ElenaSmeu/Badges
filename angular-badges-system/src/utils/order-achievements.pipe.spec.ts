import { OrderAchievementsPipe } from './order-achievements.pipe';

describe('OrderAchievementsPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderAchievementsPipe();
    expect(pipe).toBeTruthy();
  });
});
